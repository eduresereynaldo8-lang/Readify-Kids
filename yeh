TASK: Update Add Student / Student Management

1. DATABASE
Create a NEW migration only. Do not edit old migrations and do not run it automatically.

Replace the meaning of the current Student ID field with:

- lrn_no
- birthday
- age
- gender

Recommended fields:
- lrn_no: string, unique, nullable first if needed for compatibility
- birthday: date, nullable
- age: unsigned tiny integer or integer, nullable
- gender: string/enum, nullable

Do not delete existing student data.

2. LRN NO.
Change the form label:

Student ID
→ LRN No.

Use a 12-digit LRN format.

Validate:
- required
- digits:12
- unique in students table

Example placeholder:
123456789012

3. BIRTHDAY
Add a required birthday field:

<input type="date">

Birthday must not be a future date.

4. AGE
Add an Age field under Birthday.

Age must be AUTO-CALCULATED from birthday.

The field should be read-only in the UI.

Use JavaScript for live calculation, but also calculate age again in Laravel before saving so the server is authoritative.

Use Carbon, for example:
Carbon::parse($birthday)->age

Do not trust a manually submitted age value.

5. GENDER
Add Gender:

- Male
- Female

Use accessible radio buttons or styled selectable cards like the reference image.

Validate:
required
in:Male,Female

6. KEEP EXISTING FIELDS
Preserve:
- First Name
- Last Name
- Section
- Starting Level
- Username
- Password
- Confirm Password
- teacher_id
- current level
- existing user account creation

Do not break existing student login.

7. CONTROLLER
Update the actual Student Management controller that handles create/store/edit/update.

On store/update:
- validate LRN
- validate birthday
- calculate age server-side
- validate gender
- save all new fields

Preserve existing transaction/user creation logic.

8. MODEL
Update Student.php $fillable with:
- lrn_no
- birthday
- age
- gender

Add a birthday date cast if appropriate.

9. CREATE FORM
Update the current Add New Student page to match the attached image.

Layout:

First Name          Last Name
LRN No.             Birthday
Age                 Gender
Section             Starting Level
Username
Password            Confirm Password

Age is read-only.

Keep:
Cancel
Add Student

Use the existing:
@extends('layouts.teacher')

Do not recreate the sidebar/topbar.

10. EDIT STUDENT
Update the Edit Student form too.

Pre-fill:
- LRN
- Birthday
- calculated age
- Gender

Changing birthday should immediately recalculate age.

11. STUDENT LIST / MANAGEMENT
Update the Student Management table to show useful student details.

Recommended columns:
- Student
- LRN
- Age
- Gender
- Section
- Level
- Points / Status if already present
- Actions

Do not overcrowd the table on mobile; use horizontal scrolling if needed.

12. RESPONSIVE
Desktop:
two-column form like the reference image.

Mobile:
stack fields into one column.

Gender buttons should fit on small screens.

13. SAFETY
Do NOT run Git commands.
Do NOT commit or push.
Do NOT run migrations.
Do NOT run migrate:fresh / migrate:refresh.
Do NOT delete existing student records.

I will handle backup, migration, and Git manually.

14. WHEN FINISHED
Report only:
- files changed
- migration created
- columns added
- validation added
- age calculation method
- create/edit/list changes
- commands I should run manually